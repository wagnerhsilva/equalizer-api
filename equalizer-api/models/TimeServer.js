/*var mongoose = require('mongoose');
var TimeServerSchema = new mongoose.Schema({
    timeServerAddress1: String,
    timeServerAddress1_complemento: String,
    timeServerAddress2: String,
    timeServerAddress2_complemento: String,
    timeServerAddress3: String,
    timeServerAddress3_complemento: String,
    connectionRetries: Number,
    timeZone: String,
    automAdjustTimeDaylightSavingChanges: Boolean

});
module.exports = mongoose.model('TimeServer', TimeServerSchema);
*/